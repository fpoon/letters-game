spring:
  application:
    name: letters-game

  datasource:
    url: jdbc:postgresql://localhost:5432/lettersgame
    username: lettersgame
    password: lettersgame
  jpa:
    hibernate:
      ddl-auto: create-drop

  http:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB